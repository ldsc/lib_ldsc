// #include <iostream.h>#include <cmath>#include <Integral/CIGChebyshevQuadrature.h">// ------------------------------------------------------------------------------// Calcula área variando x ciclicamente.// double CIGChebyshevQuadrature::Go(double x,double y){   limiteInferior=y;   limiteSuperior=x;   Dx();  resultado= 0;  for (int i = 1; i <= numeroPontos; i++)             // Determina posição x e faz resultado=f(x)    // x=cos((2 * i - 1) * M_PI / 2 / numeroPontos);    // resultado += funcao->Go(x);   resultado += funcao->Go(cos((2 * i - 1) * M_PI / 2 / numeroPontos));  resultado=M_PI / numeroPontos * resultado; // Normaliza  return resultado;}
